class ProjectDetailService {
  /// TODO: implement — POST bid to .NET Web API / Supabase
  Future<void> submitBid({
    required String projectId,
    required double bidAmount,
  }) async {
    throw UnimplementedError();
  }
}